name: Temporary nonregular title quarantine patch

on:
  push:
    branches:
      - fix/nonregular-title-quarantine

permissions:
  contents: write

jobs:
  patch-and-test:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/nonregular-title-quarantine
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r requirements-ci.txt
      - run: python -m pip install -e .
      - run: python tools/tmp_patch_nonregular_title_quarantine.py
      - run: python -m ruff format jellyfin_show_organizer/mixed_episode_assignment.py tests/local/test_nonregular_title_quarantine.py
      - run: python -m ruff check jellyfin_show_organizer/mixed_episode_assignment.py tests/local/test_nonregular_title_quarantine.py
      - run: python -m mypy jellyfin_show_organizer
      - run: python -m pytest -q tests/local/test_nonregular_title_quarantine.py tests/local/test_explicit_episode_decision_authority.py tests/local/test_segment_counted_title_remap.py
      - name: Commit permanent patch
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add jellyfin_show_organizer/mixed_episode_assignment.py tests/local/test_nonregular_title_quarantine.py
          git commit -m 'Quarantine unnumbered special title collisions'
          git push origin HEAD:fix/nonregular-title-quarantine
