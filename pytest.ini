[pytest]
addopts =
    -ra
    --cov=jellyfin_show_organizer
    --cov-branch
    --cov-report=term-missing:skip-covered
    --cov-fail-under=84
testpaths = tests/local
markers =
    local: deterministic offline organizer tests
